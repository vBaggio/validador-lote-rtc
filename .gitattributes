* text=auto eol=lf
*.bat text eol=crlf
*.jar binary
gradlew text eol=lf

# Schemas oficiais da RFB: artefato normativo — preservar bytes exatos (fidelidade à fonte)
*.xsd -text whitespace=-trailing-space
